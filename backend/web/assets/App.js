/* frontend\src\App.svelte generated by Svelte v3.59.2 */
import {
	SvelteComponent,
	append,
	attr,
	check_outros,
	create_component,
	destroy_component,
	detach,
	element,
	empty,
	group_outros,
	init,
	insert,
	mount_component,
	noop,
	safe_not_equal,
	space,
	transition_in,
	transition_out
} from "svelte/internal";

import { onMount } from 'svelte';
import Sidebar from './components/Sidebar.js';
import Dashboard from './pages/Dashboard.js';
import ProxyList from './pages/ProxyList.js';
import Settings from './pages/Settings.js';
import Login from './pages/Login.js';
import Setup from './pages/Setup.js';
import Password from './pages/Password.js';

function create_else_block_1(ctx) {
	let div;
	let sidebar;
	let t;
	let main;
	let current_block_type_index;
	let if_block;
	let current;

	sidebar = new Sidebar({
			props: {
				activePath: /*route*/ ctx[0],
				onNavigate: /*navigate*/ ctx[3]
			}
		});

	const if_block_creators = [create_if_block_2, create_if_block_3, create_if_block_4, create_else_block_2];
	const if_blocks = [];

	function select_block_type_2(ctx, dirty) {
		if (/*route*/ ctx[0] === '/accounts') return 0;
		if (/*route*/ ctx[0] === '/settings') return 1;
		if (/*route*/ ctx[0] === '/settings/password') return 2;
		return 3;
	}

	current_block_type_index = select_block_type_2(ctx, -1);
	if_block = if_blocks[current_block_type_index] = if_block_creators[current_block_type_index](ctx);

	return {
		c() {
			div = element("div");
			create_component(sidebar.$$.fragment);
			t = space();
			main = element("main");
			if_block.c();
			attr(main, "class", "container");
			attr(div, "class", "app-shell");
		},
		m(target, anchor) {
			insert(target, div, anchor);
			mount_component(sidebar, div, null);
			append(div, t);
			append(div, main);
			if_blocks[current_block_type_index].m(main, null);
			current = true;
		},
		p(ctx, dirty) {
			const sidebar_changes = {};
			if (dirty & /*route*/ 1) sidebar_changes.activePath = /*route*/ ctx[0];
			sidebar.$set(sidebar_changes);
			let previous_block_index = current_block_type_index;
			current_block_type_index = select_block_type_2(ctx, dirty);

			if (current_block_type_index === previous_block_index) {
				if_blocks[current_block_type_index].p(ctx, dirty);
			} else {
				group_outros();

				transition_out(if_blocks[previous_block_index], 1, 1, () => {
					if_blocks[previous_block_index] = null;
				});

				check_outros();
				if_block = if_blocks[current_block_type_index];

				if (!if_block) {
					if_block = if_blocks[current_block_type_index] = if_block_creators[current_block_type_index](ctx);
					if_block.c();
				} else {
					if_block.p(ctx, dirty);
				}

				transition_in(if_block, 1);
				if_block.m(main, null);
			}
		},
		i(local) {
			if (current) return;
			transition_in(sidebar.$$.fragment, local);
			transition_in(if_block);
			current = true;
		},
		o(local) {
			transition_out(sidebar.$$.fragment, local);
			transition_out(if_block);
			current = false;
		},
		d(detaching) {
			if (detaching) detach(div);
			destroy_component(sidebar);
			if_blocks[current_block_type_index].d();
		}
	};
}

// (36:0) {#if authPage}
function create_if_block(ctx) {
	let current_block_type_index;
	let if_block;
	let if_block_anchor;
	let current;
	const if_block_creators = [create_if_block_1, create_else_block];
	const if_blocks = [];

	function select_block_type_1(ctx, dirty) {
		if (/*route*/ ctx[0] === '/setup') return 0;
		return 1;
	}

	current_block_type_index = select_block_type_1(ctx, -1);
	if_block = if_blocks[current_block_type_index] = if_block_creators[current_block_type_index](ctx);

	return {
		c() {
			if_block.c();
			if_block_anchor = empty();
		},
		m(target, anchor) {
			if_blocks[current_block_type_index].m(target, anchor);
			insert(target, if_block_anchor, anchor);
			current = true;
		},
		p(ctx, dirty) {
			let previous_block_index = current_block_type_index;
			current_block_type_index = select_block_type_1(ctx, dirty);

			if (current_block_type_index === previous_block_index) {
				if_blocks[current_block_type_index].p(ctx, dirty);
			} else {
				group_outros();

				transition_out(if_blocks[previous_block_index], 1, 1, () => {
					if_blocks[previous_block_index] = null;
				});

				check_outros();
				if_block = if_blocks[current_block_type_index];

				if (!if_block) {
					if_block = if_blocks[current_block_type_index] = if_block_creators[current_block_type_index](ctx);
					if_block.c();
				} else {
					if_block.p(ctx, dirty);
				}

				transition_in(if_block, 1);
				if_block.m(if_block_anchor.parentNode, if_block_anchor);
			}
		},
		i(local) {
			if (current) return;
			transition_in(if_block);
			current = true;
		},
		o(local) {
			transition_out(if_block);
			current = false;
		},
		d(detaching) {
			if_blocks[current_block_type_index].d(detaching);
			if (detaching) detach(if_block_anchor);
		}
	};
}

// (52:6) {:else}
function create_else_block_2(ctx) {
	let dashboard;
	let current;
	dashboard = new Dashboard({});

	return {
		c() {
			create_component(dashboard.$$.fragment);
		},
		m(target, anchor) {
			mount_component(dashboard, target, anchor);
			current = true;
		},
		p: noop,
		i(local) {
			if (current) return;
			transition_in(dashboard.$$.fragment, local);
			current = true;
		},
		o(local) {
			transition_out(dashboard.$$.fragment, local);
			current = false;
		},
		d(detaching) {
			destroy_component(dashboard, detaching);
		}
	};
}

// (50:47) 
function create_if_block_4(ctx) {
	let password;
	let current;
	password = new Password({ props: { search: /*search*/ ctx[1] } });

	return {
		c() {
			create_component(password.$$.fragment);
		},
		m(target, anchor) {
			mount_component(password, target, anchor);
			current = true;
		},
		p(ctx, dirty) {
			const password_changes = {};
			if (dirty & /*search*/ 2) password_changes.search = /*search*/ ctx[1];
			password.$set(password_changes);
		},
		i(local) {
			if (current) return;
			transition_in(password.$$.fragment, local);
			current = true;
		},
		o(local) {
			transition_out(password.$$.fragment, local);
			current = false;
		},
		d(detaching) {
			destroy_component(password, detaching);
		}
	};
}

// (48:38) 
function create_if_block_3(ctx) {
	let settings;
	let current;
	settings = new Settings({});

	return {
		c() {
			create_component(settings.$$.fragment);
		},
		m(target, anchor) {
			mount_component(settings, target, anchor);
			current = true;
		},
		p: noop,
		i(local) {
			if (current) return;
			transition_in(settings.$$.fragment, local);
			current = true;
		},
		o(local) {
			transition_out(settings.$$.fragment, local);
			current = false;
		},
		d(detaching) {
			destroy_component(settings, detaching);
		}
	};
}

// (46:6) {#if route === '/accounts'}
function create_if_block_2(ctx) {
	let proxylist;
	let current;
	proxylist = new ProxyList({});

	return {
		c() {
			create_component(proxylist.$$.fragment);
		},
		m(target, anchor) {
			mount_component(proxylist, target, anchor);
			current = true;
		},
		p: noop,
		i(local) {
			if (current) return;
			transition_in(proxylist.$$.fragment, local);
			current = true;
		},
		o(local) {
			transition_out(proxylist.$$.fragment, local);
			current = false;
		},
		d(detaching) {
			destroy_component(proxylist, detaching);
		}
	};
}

// (39:2) {:else}
function create_else_block(ctx) {
	let login;
	let current;
	login = new Login({ props: { search: /*search*/ ctx[1] } });

	return {
		c() {
			create_component(login.$$.fragment);
		},
		m(target, anchor) {
			mount_component(login, target, anchor);
			current = true;
		},
		p(ctx, dirty) {
			const login_changes = {};
			if (dirty & /*search*/ 2) login_changes.search = /*search*/ ctx[1];
			login.$set(login_changes);
		},
		i(local) {
			if (current) return;
			transition_in(login.$$.fragment, local);
			current = true;
		},
		o(local) {
			transition_out(login.$$.fragment, local);
			current = false;
		},
		d(detaching) {
			destroy_component(login, detaching);
		}
	};
}

// (37:2) {#if route === '/setup'}
function create_if_block_1(ctx) {
	let setup;
	let current;
	setup = new Setup({ props: { search: /*search*/ ctx[1] } });

	return {
		c() {
			create_component(setup.$$.fragment);
		},
		m(target, anchor) {
			mount_component(setup, target, anchor);
			current = true;
		},
		p(ctx, dirty) {
			const setup_changes = {};
			if (dirty & /*search*/ 2) setup_changes.search = /*search*/ ctx[1];
			setup.$set(setup_changes);
		},
		i(local) {
			if (current) return;
			transition_in(setup.$$.fragment, local);
			current = true;
		},
		o(local) {
			transition_out(setup.$$.fragment, local);
			current = false;
		},
		d(detaching) {
			destroy_component(setup, detaching);
		}
	};
}

function create_fragment(ctx) {
	let current_block_type_index;
	let if_block;
	let if_block_anchor;
	let current;
	const if_block_creators = [create_if_block, create_else_block_1];
	const if_blocks = [];

	function select_block_type(ctx, dirty) {
		if (/*authPage*/ ctx[2]) return 0;
		return 1;
	}

	current_block_type_index = select_block_type(ctx, -1);
	if_block = if_blocks[current_block_type_index] = if_block_creators[current_block_type_index](ctx);

	return {
		c() {
			if_block.c();
			if_block_anchor = empty();
		},
		m(target, anchor) {
			if_blocks[current_block_type_index].m(target, anchor);
			insert(target, if_block_anchor, anchor);
			current = true;
		},
		p(ctx, [dirty]) {
			let previous_block_index = current_block_type_index;
			current_block_type_index = select_block_type(ctx, dirty);

			if (current_block_type_index === previous_block_index) {
				if_blocks[current_block_type_index].p(ctx, dirty);
			} else {
				group_outros();

				transition_out(if_blocks[previous_block_index], 1, 1, () => {
					if_blocks[previous_block_index] = null;
				});

				check_outros();
				if_block = if_blocks[current_block_type_index];

				if (!if_block) {
					if_block = if_blocks[current_block_type_index] = if_block_creators[current_block_type_index](ctx);
					if_block.c();
				} else {
					if_block.p(ctx, dirty);
				}

				transition_in(if_block, 1);
				if_block.m(if_block_anchor.parentNode, if_block_anchor);
			}
		},
		i(local) {
			if (current) return;
			transition_in(if_block);
			current = true;
		},
		o(local) {
			transition_out(if_block);
			current = false;
		},
		d(detaching) {
			if_blocks[current_block_type_index].d(detaching);
			if (detaching) detach(if_block_anchor);
		}
	};
}

function instance($$self, $$props, $$invalidate) {
	let route;
	let authPage;
	let path = window.location.pathname;
	let search = window.location.search;

	function syncLocation() {
		$$invalidate(4, path = window.location.pathname);
		$$invalidate(1, search = window.location.search);
	}

	function navigate(event, href) {
		event.preventDefault();

		if (window.location.pathname !== href) {
			history.pushState({}, '', href);
			syncLocation();
		}
	}

	onMount(() => {
		window.addEventListener('popstate', syncLocation);
		return () => window.removeEventListener('popstate', syncLocation);
	});

	$$self.$$.update = () => {
		if ($$self.$$.dirty & /*path*/ 16) {
			$: $$invalidate(0, route = path === '/accounts' || path === '/settings' || path === '/settings/password' || path === '/login' || path === '/setup'
			? path
			: '/');
		}

		if ($$self.$$.dirty & /*route*/ 1) {
			$: $$invalidate(2, authPage = route === '/login' || route === '/setup');
		}
	};

	return [route, search, authPage, navigate, path];
}

class App extends SvelteComponent {
	constructor(options) {
		super();
		init(this, options, instance, create_fragment, safe_not_equal, {});
	}
}

export default App;