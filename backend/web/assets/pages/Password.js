/* src\pages\Password.svelte generated by Svelte v3.59.2 */
import {
	SvelteComponent,
	append,
	attr,
	detach,
	element,
	init,
	insert,
	noop,
	safe_not_equal,
	set_data,
	space,
	text
} from "svelte/internal";

function create_if_block(ctx) {
	let div;
	let t;

	return {
		c() {
			div = element("div");
			t = text(/*error*/ ctx[0]);
			attr(div, "class", "alert");
		},
		m(target, anchor) {
			insert(target, div, anchor);
			append(div, t);
		},
		p(ctx, dirty) {
			if (dirty & /*error*/ 1) set_data(t, /*error*/ ctx[0]);
		},
		d(detaching) {
			if (detaching) detach(div);
		}
	};
}

function create_fragment(ctx) {
	let h2;
	let t1;
	let t2;
	let form;
	let if_block = /*error*/ ctx[0] && create_if_block(ctx);

	return {
		c() {
			h2 = element("h2");
			h2.textContent = "修改后台账号";
			t1 = space();
			if (if_block) if_block.c();
			t2 = space();
			form = element("form");

			form.innerHTML = `<label>新用户名（留空不变）
    <input type="text" name="username" minlength="3"/></label> 
  <label>旧密码
    <input type="password" name="old_password" required=""/></label> 
  <label>新密码（至少 8 位）
    <input type="password" name="new_password" required="" minlength="8"/></label> 
  <label>确认新密码
    <input type="password" name="confirm_password" required="" minlength="8"/></label> 
  <button type="submit">保存账号</button>`;

			attr(form, "method", "post");
			attr(form, "action", "/settings/password");
			attr(form, "class", "settings-form");
		},
		m(target, anchor) {
			insert(target, h2, anchor);
			insert(target, t1, anchor);
			if (if_block) if_block.m(target, anchor);
			insert(target, t2, anchor);
			insert(target, form, anchor);
		},
		p(ctx, [dirty]) {
			if (/*error*/ ctx[0]) {
				if (if_block) {
					if_block.p(ctx, dirty);
				} else {
					if_block = create_if_block(ctx);
					if_block.c();
					if_block.m(t2.parentNode, t2);
				}
			} else if (if_block) {
				if_block.d(1);
				if_block = null;
			}
		},
		i: noop,
		o: noop,
		d(detaching) {
			if (detaching) detach(h2);
			if (detaching) detach(t1);
			if (if_block) if_block.d(detaching);
			if (detaching) detach(t2);
			if (detaching) detach(form);
		}
	};
}

function instance($$self, $$props, $$invalidate) {
	let error;
	let { search = '' } = $$props;

	$$self.$$set = $$props => {
		if ('search' in $$props) $$invalidate(1, search = $$props.search);
	};

	$$self.$$.update = () => {
		if ($$self.$$.dirty & /*search*/ 2) {
			$: $$invalidate(0, error = new URLSearchParams(search).get('error') || '');
		}
	};

	return [error, search];
}

class Password extends SvelteComponent {
	constructor(options) {
		super();
		init(this, options, instance, create_fragment, safe_not_equal, { search: 1 });
	}
}

export default Password;