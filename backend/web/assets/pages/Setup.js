/* src\pages\Setup.svelte generated by Svelte v3.59.2 */
import {
	SvelteComponent,
	append,
	attr,
	detach,
	element,
	init,
	insert,
	noop,
	safe_not_equal,
	set_data,
	space,
	text
} from "svelte/internal";

function create_if_block(ctx) {
	let div;
	let t;

	return {
		c() {
			div = element("div");
			t = text(/*error*/ ctx[0]);
			attr(div, "class", "alert");
		},
		m(target, anchor) {
			insert(target, div, anchor);
			append(div, t);
		},
		p(ctx, dirty) {
			if (dirty & /*error*/ 1) set_data(t, /*error*/ ctx[0]);
		},
		d(detaching) {
			if (detaching) detach(div);
		}
	};
}

function create_fragment(ctx) {
	let div;
	let form;
	let h1;
	let t1;
	let p;
	let t3;
	let t4;
	let label0;
	let t6;
	let label1;
	let t8;
	let label2;
	let t10;
	let button;
	let if_block = /*error*/ ctx[0] && create_if_block(ctx);

	return {
		c() {
			div = element("div");
			form = element("form");
			h1 = element("h1");
			h1.textContent = "ProxyForge";
			t1 = space();
			p = element("p");
			p.textContent = "首次使用需要初始化后台账号。数据库、默认设置和代理槽位会自动创建。";
			t3 = space();
			if (if_block) if_block.c();
			t4 = space();
			label0 = element("label");

			label0.innerHTML = `用户名
      <input type="text" name="username" required="" minlength="3" autofocus=""/>`;

			t6 = space();
			label1 = element("label");

			label1.innerHTML = `密码
      <input type="password" name="password" required="" minlength="12" autocomplete="new-password"/>`;

			t8 = space();
			label2 = element("label");

			label2.innerHTML = `确认密码
      <input type="password" name="confirm_password" required="" minlength="12" autocomplete="new-password"/>`;

			t10 = space();
			button = element("button");
			button.textContent = "初始化并登录";
			attr(p, "class", "hint");
			attr(button, "type", "submit");
			attr(form, "class", "login-card");
			attr(form, "method", "post");
			attr(form, "action", "/setup");
			attr(div, "class", "login-wrapper");
		},
		m(target, anchor) {
			insert(target, div, anchor);
			append(div, form);
			append(form, h1);
			append(form, t1);
			append(form, p);
			append(form, t3);
			if (if_block) if_block.m(form, null);
			append(form, t4);
			append(form, label0);
			append(form, t6);
			append(form, label1);
			append(form, t8);
			append(form, label2);
			append(form, t10);
			append(form, button);
		},
		p(ctx, [dirty]) {
			if (/*error*/ ctx[0]) {
				if (if_block) {
					if_block.p(ctx, dirty);
				} else {
					if_block = create_if_block(ctx);
					if_block.c();
					if_block.m(form, t4);
				}
			} else if (if_block) {
				if_block.d(1);
				if_block = null;
			}
		},
		i: noop,
		o: noop,
		d(detaching) {
			if (detaching) detach(div);
			if (if_block) if_block.d();
		}
	};
}

function instance($$self, $$props, $$invalidate) {
	let error;
	let { search = '' } = $$props;

	$$self.$$set = $$props => {
		if ('search' in $$props) $$invalidate(1, search = $$props.search);
	};

	$$self.$$.update = () => {
		if ($$self.$$.dirty & /*search*/ 2) {
			$: $$invalidate(0, error = new URLSearchParams(search).get('error') || '');
		}
	};

	return [error, search];
}

class Setup extends SvelteComponent {
	constructor(options) {
		super();
		init(this, options, instance, create_fragment, safe_not_equal, { search: 1 });
	}
}

export default Setup;