/* src\pages\Settings.svelte generated by Svelte v3.59.2 */
import {
	SvelteComponent,
	add_render_callback,
	append,
	attr,
	detach,
	element,
	init,
	insert,
	listen,
	noop,
	prevent_default,
	run_all,
	safe_not_equal,
	select_option,
	select_value,
	set_data,
	set_input_value,
	space,
	text,
	to_number
} from "svelte/internal";

import { onMount } from 'svelte';
import { fetchJSON } from '../lib/api.js';

function create_if_block_1(ctx) {
	let div;

	return {
		c() {
			div = element("div");
			div.textContent = "已保存";
			attr(div, "class", "alert success");
		},
		m(target, anchor) {
			insert(target, div, anchor);
		},
		d(detaching) {
			if (detaching) detach(div);
		}
	};
}

// (55:0) {#if error}
function create_if_block(ctx) {
	let div;
	let t0;
	let t1;

	return {
		c() {
			div = element("div");
			t0 = text("保存失败：");
			t1 = text(/*error*/ ctx[1]);
			attr(div, "class", "alert");
		},
		m(target, anchor) {
			insert(target, div, anchor);
			append(div, t0);
			append(div, t1);
		},
		p(ctx, dirty) {
			if (dirty & /*error*/ 2) set_data(t1, /*error*/ ctx[1]);
		},
		d(detaching) {
			if (detaching) detach(div);
		}
	};
}

function create_fragment(ctx) {
	let h2;
	let t1;
	let t2;
	let t3;
	let form;
	let fieldset0;
	let legend0;
	let t5;
	let label0;
	let t6;
	let input0;
	let t7;
	let label1;
	let t8;
	let select0;
	let option0;
	let option1;
	let t11;
	let p0;
	let t13;
	let fieldset1;
	let legend1;
	let t15;
	let label2;
	let t16;
	let input1;
	let t17;
	let label3;
	let t18;
	let select1;
	let option2;
	let option3;
	let t21;
	let label4;
	let t22;
	let input2;
	let t23;
	let label5;
	let t24;
	let input3;
	let t25;
	let p1;
	let t27;
	let label6;
	let t28;
	let select2;
	let option4;
	let option5;
	let t31;
	let p2;
	let t33;
	let fieldset2;
	let legend2;
	let t35;
	let label7;
	let t36;
	let input4;
	let t37;
	let p3;
	let t39;
	let div;
	let button;
	let t40_value = (/*saving*/ ctx[3] ? '保存中...' : '保存设置') + "";
	let t40;
	let t41;
	let a;
	let mounted;
	let dispose;
	let if_block0 = /*saved*/ ctx[2] && create_if_block_1(ctx);
	let if_block1 = /*error*/ ctx[1] && create_if_block(ctx);

	return {
		c() {
			h2 = element("h2");
			h2.textContent = "代理设置";
			t1 = space();
			if (if_block0) if_block0.c();
			t2 = space();
			if (if_block1) if_block1.c();
			t3 = space();
			form = element("form");
			fieldset0 = element("fieldset");
			legend0 = element("legend");
			legend0.textContent = "代理规模";
			t5 = space();
			label0 = element("label");
			t6 = text("代理 IP 数\n      ");
			input0 = element("input");
			t7 = space();
			label1 = element("label");
			t8 = text("自动生成 WARP\n      ");
			select0 = element("select");
			option0 = element("option");
			option0.textContent = "开启";
			option1 = element("option");
			option1.textContent = "关闭";
			t11 = space();
			p0 = element("p");
			p0.textContent = "设置需要长期使用的固定代理 IP 数。ProxyForge 会自动准备额外 WARP 候补，例如 20 个代理 IP 会自动维护更多候选节点。";
			t13 = space();
			fieldset1 = element("fieldset");
			legend1 = element("legend");
			legend1.textContent = "代理监听";
			t15 = space();
			label2 = element("label");
			t16 = text("代理端口\n      ");
			input1 = element("input");
			t17 = space();
			label3 = element("label");
			t18 = text("监听地址\n      ");
			select1 = element("select");
			option2 = element("option");
			option2.textContent = "0.0.0.0";
			option3 = element("option");
			option3.textContent = "127.0.0.1";
			t21 = space();
			label4 = element("label");
			t22 = text("全局密码\n      ");
			input2 = element("input");
			t23 = space();
			label5 = element("label");
			t24 = text("代理对外地址\n      ");
			input3 = element("input");
			t25 = space();
			p1 = element("p");
			p1.textContent = "仅用于手动复制旧版 HTTP / SOCKS5 代理。Clash / Mihomo 订阅固定使用当前面板 HTTPS 域名的 443 端口，通过 Trojan + TLS + WebSocket 加密传输。";
			t27 = space();
			label6 = element("label");
			t28 = text("传输加密（TLS）\n      ");
			select2 = element("select");
			option4 = element("option");
			option4.textContent = "开启（推荐）";
			option5 = element("option");
			option5.textContent = "关闭";
			t31 = space();
			p2 = element("p");
			p2.textContent = "控制旧版代理端口是否额外接受 HTTPS Proxy；不会影响 Clash 订阅。Clash / Mihomo 始终使用由宝塔 nginx 提供可信证书的 Trojan + WSS，不会在公网传输明文 CONNECT。";
			t33 = space();
			fieldset2 = element("fieldset");
			legend2 = element("legend");
			legend2.textContent = "自动检测";
			t35 = space();
			label7 = element("label");
			t36 = text("检测和修复间隔（秒）\n      ");
			input4 = element("input");
			t37 = space();
			p3 = element("p");
			p3.textContent = "节点生成、测试、去重、失败重绑由后台自动处理，通常只需要调整代理 IP 数。";
			t39 = space();
			div = element("div");
			button = element("button");
			t40 = text(t40_value);
			t41 = space();
			a = element("a");
			a.textContent = "修改后台账号";
			attr(input0, "type", "number");
			attr(input0, "min", "1");
			option0.__value = "on";
			option0.value = option0.__value;
			option1.__value = "off";
			option1.value = option1.__value;
			if (/*settings*/ ctx[0].auto_generation === void 0) add_render_callback(() => /*select0_change_handler*/ ctx[6].call(select0));
			attr(p0, "class", "hint");
			attr(input1, "type", "number");
			option2.__value = "0.0.0.0";
			option2.value = option2.__value;
			option3.__value = "127.0.0.1";
			option3.value = option3.__value;
			if (/*settings*/ ctx[0].proxy_listen_addr === void 0) add_render_callback(() => /*select1_change_handler*/ ctx[8].call(select1));
			attr(input2, "type", "password");
			attr(input2, "minlength", "16");
			attr(input2, "autocomplete", "new-password");
			attr(input3, "type", "text");
			attr(input3, "placeholder", "留空则用当前访问域名");
			attr(p1, "class", "hint");
			option4.__value = "on";
			option4.value = option4.__value;
			option5.__value = "off";
			option5.value = option5.__value;
			if (/*settings*/ ctx[0].proxy_tls === void 0) add_render_callback(() => /*select2_change_handler*/ ctx[11].call(select2));
			attr(p2, "class", "hint");
			attr(input4, "type", "number");
			attr(p3, "class", "hint");
			attr(button, "type", "submit");
			button.disabled = /*saving*/ ctx[3];
			attr(a, "class", "secondary-link");
			attr(a, "href", "/settings/password");
			attr(div, "class", "form-actions");
			attr(form, "class", "settings-form");
		},
		m(target, anchor) {
			insert(target, h2, anchor);
			insert(target, t1, anchor);
			if (if_block0) if_block0.m(target, anchor);
			insert(target, t2, anchor);
			if (if_block1) if_block1.m(target, anchor);
			insert(target, t3, anchor);
			insert(target, form, anchor);
			append(form, fieldset0);
			append(fieldset0, legend0);
			append(fieldset0, t5);
			append(fieldset0, label0);
			append(label0, t6);
			append(label0, input0);
			set_input_value(input0, /*settings*/ ctx[0].proxy_slot_count);
			append(fieldset0, t7);
			append(fieldset0, label1);
			append(label1, t8);
			append(label1, select0);
			append(select0, option0);
			append(select0, option1);
			select_option(select0, /*settings*/ ctx[0].auto_generation, true);
			append(fieldset0, t11);
			append(fieldset0, p0);
			append(form, t13);
			append(form, fieldset1);
			append(fieldset1, legend1);
			append(fieldset1, t15);
			append(fieldset1, label2);
			append(label2, t16);
			append(label2, input1);
			set_input_value(input1, /*settings*/ ctx[0].proxy_port);
			append(fieldset1, t17);
			append(fieldset1, label3);
			append(label3, t18);
			append(label3, select1);
			append(select1, option2);
			append(select1, option3);
			select_option(select1, /*settings*/ ctx[0].proxy_listen_addr, true);
			append(fieldset1, t21);
			append(fieldset1, label4);
			append(label4, t22);
			append(label4, input2);
			set_input_value(input2, /*settings*/ ctx[0].proxy_password);
			append(fieldset1, t23);
			append(fieldset1, label5);
			append(label5, t24);
			append(label5, input3);
			set_input_value(input3, /*settings*/ ctx[0].proxy_public_host);
			append(fieldset1, t25);
			append(fieldset1, p1);
			append(fieldset1, t27);
			append(fieldset1, label6);
			append(label6, t28);
			append(label6, select2);
			append(select2, option4);
			append(select2, option5);
			select_option(select2, /*settings*/ ctx[0].proxy_tls, true);
			append(fieldset1, t31);
			append(fieldset1, p2);
			append(form, t33);
			append(form, fieldset2);
			append(fieldset2, legend2);
			append(fieldset2, t35);
			append(fieldset2, label7);
			append(label7, t36);
			append(label7, input4);
			set_input_value(input4, /*settings*/ ctx[0].dedup_interval_seconds);
			append(fieldset2, t37);
			append(fieldset2, p3);
			append(form, t39);
			append(form, div);
			append(div, button);
			append(button, t40);
			append(div, t41);
			append(div, a);

			if (!mounted) {
				dispose = [
					listen(input0, "input", /*input0_input_handler*/ ctx[5]),
					listen(select0, "change", /*select0_change_handler*/ ctx[6]),
					listen(input1, "input", /*input1_input_handler*/ ctx[7]),
					listen(select1, "change", /*select1_change_handler*/ ctx[8]),
					listen(input2, "input", /*input2_input_handler*/ ctx[9]),
					listen(input3, "input", /*input3_input_handler*/ ctx[10]),
					listen(select2, "change", /*select2_change_handler*/ ctx[11]),
					listen(input4, "input", /*input4_input_handler*/ ctx[12]),
					listen(form, "submit", prevent_default(/*saveSettings*/ ctx[4]))
				];

				mounted = true;
			}
		},
		p(ctx, [dirty]) {
			if (/*saved*/ ctx[2]) {
				if (if_block0) {
					
				} else {
					if_block0 = create_if_block_1(ctx);
					if_block0.c();
					if_block0.m(t2.parentNode, t2);
				}
			} else if (if_block0) {
				if_block0.d(1);
				if_block0 = null;
			}

			if (/*error*/ ctx[1]) {
				if (if_block1) {
					if_block1.p(ctx, dirty);
				} else {
					if_block1 = create_if_block(ctx);
					if_block1.c();
					if_block1.m(t3.parentNode, t3);
				}
			} else if (if_block1) {
				if_block1.d(1);
				if_block1 = null;
			}

			if (dirty & /*settings*/ 1 && to_number(input0.value) !== /*settings*/ ctx[0].proxy_slot_count) {
				set_input_value(input0, /*settings*/ ctx[0].proxy_slot_count);
			}

			if (dirty & /*settings*/ 1) {
				select_option(select0, /*settings*/ ctx[0].auto_generation);
			}

			if (dirty & /*settings*/ 1 && to_number(input1.value) !== /*settings*/ ctx[0].proxy_port) {
				set_input_value(input1, /*settings*/ ctx[0].proxy_port);
			}

			if (dirty & /*settings*/ 1) {
				select_option(select1, /*settings*/ ctx[0].proxy_listen_addr);
			}

			if (dirty & /*settings*/ 1 && input2.value !== /*settings*/ ctx[0].proxy_password) {
				set_input_value(input2, /*settings*/ ctx[0].proxy_password);
			}

			if (dirty & /*settings*/ 1 && input3.value !== /*settings*/ ctx[0].proxy_public_host) {
				set_input_value(input3, /*settings*/ ctx[0].proxy_public_host);
			}

			if (dirty & /*settings*/ 1) {
				select_option(select2, /*settings*/ ctx[0].proxy_tls);
			}

			if (dirty & /*settings*/ 1 && to_number(input4.value) !== /*settings*/ ctx[0].dedup_interval_seconds) {
				set_input_value(input4, /*settings*/ ctx[0].dedup_interval_seconds);
			}

			if (dirty & /*saving*/ 8 && t40_value !== (t40_value = (/*saving*/ ctx[3] ? '保存中...' : '保存设置') + "")) set_data(t40, t40_value);

			if (dirty & /*saving*/ 8) {
				button.disabled = /*saving*/ ctx[3];
			}
		},
		i: noop,
		o: noop,
		d(detaching) {
			if (detaching) detach(h2);
			if (detaching) detach(t1);
			if (if_block0) if_block0.d(detaching);
			if (detaching) detach(t2);
			if (if_block1) if_block1.d(detaching);
			if (detaching) detach(t3);
			if (detaching) detach(form);
			mounted = false;
			run_all(dispose);
		}
	};
}

function instance($$self, $$props, $$invalidate) {
	const settingKeys = [
		'proxy_slot_count',
		'auto_generation',
		'proxy_port',
		'proxy_listen_addr',
		'proxy_public_host',
		'proxy_password',
		'proxy_tls',
		'dedup_interval_seconds'
	];

	let settings = {};
	let error = '';
	let saved = new URLSearchParams(window.location.search).get('saved') === '1';
	let saving = false;

	async function loadSettings() {
		try {
			const data = await fetchJSON('/api/settings/json');
			$$invalidate(0, settings = data.settings || {});
			$$invalidate(1, error = '');
		} catch(err) {
			$$invalidate(1, error = err.message);
		}
	}

	async function saveSettings() {
		$$invalidate(3, saving = true);
		$$invalidate(1, error = '');
		$$invalidate(2, saved = false);

		try {
			const body = new URLSearchParams();

			for (const key of settingKeys) {
				body.set(key, settings[key] == null ? '' : String(settings[key]));
			}

			await fetchJSON('/api/settings', { method: 'POST', body });
			$$invalidate(2, saved = true);
			await loadSettings();
		} catch(err) {
			$$invalidate(1, error = err.message);
		} finally {
			$$invalidate(3, saving = false);
		}
	}

	onMount(loadSettings);

	function input0_input_handler() {
		settings.proxy_slot_count = to_number(this.value);
		$$invalidate(0, settings);
	}

	function select0_change_handler() {
		settings.auto_generation = select_value(this);
		$$invalidate(0, settings);
	}

	function input1_input_handler() {
		settings.proxy_port = to_number(this.value);
		$$invalidate(0, settings);
	}

	function select1_change_handler() {
		settings.proxy_listen_addr = select_value(this);
		$$invalidate(0, settings);
	}

	function input2_input_handler() {
		settings.proxy_password = this.value;
		$$invalidate(0, settings);
	}

	function input3_input_handler() {
		settings.proxy_public_host = this.value;
		$$invalidate(0, settings);
	}

	function select2_change_handler() {
		settings.proxy_tls = select_value(this);
		$$invalidate(0, settings);
	}

	function input4_input_handler() {
		settings.dedup_interval_seconds = to_number(this.value);
		$$invalidate(0, settings);
	}

	return [
		settings,
		error,
		saved,
		saving,
		saveSettings,
		input0_input_handler,
		select0_change_handler,
		input1_input_handler,
		select1_change_handler,
		input2_input_handler,
		input3_input_handler,
		select2_change_handler,
		input4_input_handler
	];
}

class Settings extends SvelteComponent {
	constructor(options) {
		super();
		init(this, options, instance, create_fragment, safe_not_equal, {});
	}
}

export default Settings;