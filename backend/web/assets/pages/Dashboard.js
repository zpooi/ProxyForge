/* frontend\src\pages\Dashboard.svelte generated by Svelte v3.59.2 */
import {
	SvelteComponent,
	append,
	attr,
	check_outros,
	create_component,
	destroy_component,
	detach,
	element,
	empty,
	group_outros,
	init,
	insert,
	mount_component,
	noop,
	safe_not_equal,
	set_data,
	space,
	text,
	transition_in,
	transition_out
} from "svelte/internal";

import { onMount } from 'svelte';
import StatCard from '../components/StatCard.js';
import { fetchJSON } from '../lib/api.js';
import { fmtBytes, fmtTime } from '../lib/format.js';

function create_else_block(ctx) {
	let div;

	return {
		c() {
			div = element("div");
			div.textContent = "正在加载仪表盘...";
			attr(div, "class", "alert");
		},
		m(target, anchor) {
			insert(target, div, anchor);
		},
		p: noop,
		i: noop,
		o: noop,
		d(detaching) {
			if (detaching) detach(div);
		}
	};
}

// (32:20) 
function create_if_block_1(ctx) {
	let div;
	let statcard0;
	let t0;
	let statcard1;
	let t1;
	let statcard2;
	let t2;
	let statcard3;
	let t3;
	let statcard4;
	let t4;
	let statcard5;
	let t5;
	let statcard6;
	let t6;
	let statcard7;
	let t7;
	let statcard8;
	let t8;
	let statcard9;
	let current;

	statcard0 = new StatCard({
			props: {
				label: "代理 IP 数",
				value: /*dashboard*/ ctx[0].proxy_slots || 0
			}
		});

	statcard1 = new StatCard({
			props: {
				label: "可用 WARP",
				value: /*dashboard*/ ctx[0].active || 0,
				sub: '目标池：' + (/*dashboard*/ ctx[0].target_pool || 0)
			}
		});

	statcard2 = new StatCard({
			props: {
				label: "唯一出口 IP",
				value: /*dashboard*/ ctx[0].unique_ips || 0
			}
		});

	statcard3 = new StatCard({
			props: {
				label: "运行隧道",
				value: /*dashboard*/ ctx[0].running_tunnels || 0
			}
		});

	statcard4 = new StatCard({
			props: {
				label: "代理端口",
				value: /*dashboard*/ ctx[0].proxy_port || '-'
			}
		});

	statcard5 = new StatCard({
			props: {
				label: "WARP 总数",
				value: /*dashboard*/ ctx[0].total || 0,
				sub: '停用：' + (/*dashboard*/ ctx[0].disabled || 0) + ' / 错误：' + (/*dashboard*/ ctx[0].error || 0)
			}
		});

	statcard6 = new StatCard({
			props: {
				label: "总上行",
				value: fmtBytes(/*dashboard*/ ctx[0].total_up || 0)
			}
		});

	statcard7 = new StatCard({
			props: {
				label: "总下行",
				value: fmtBytes(/*dashboard*/ ctx[0].total_down || 0)
			}
		});

	statcard8 = new StatCard({
			props: {
				label: "后台状态",
				value: /*dashboard*/ ctx[0].running ? '运行中' : '空闲',
				sub: '上次：' + /*lastRun*/ ctx[3]
			}
		});

	statcard9 = new StatCard({
			props: {
				label: "下次自动检测",
				value: /*nextRun*/ ctx[2]
			}
		});

	return {
		c() {
			div = element("div");
			create_component(statcard0.$$.fragment);
			t0 = space();
			create_component(statcard1.$$.fragment);
			t1 = space();
			create_component(statcard2.$$.fragment);
			t2 = space();
			create_component(statcard3.$$.fragment);
			t3 = space();
			create_component(statcard4.$$.fragment);
			t4 = space();
			create_component(statcard5.$$.fragment);
			t5 = space();
			create_component(statcard6.$$.fragment);
			t6 = space();
			create_component(statcard7.$$.fragment);
			t7 = space();
			create_component(statcard8.$$.fragment);
			t8 = space();
			create_component(statcard9.$$.fragment);
			attr(div, "class", "dashboard-grid");
		},
		m(target, anchor) {
			insert(target, div, anchor);
			mount_component(statcard0, div, null);
			append(div, t0);
			mount_component(statcard1, div, null);
			append(div, t1);
			mount_component(statcard2, div, null);
			append(div, t2);
			mount_component(statcard3, div, null);
			append(div, t3);
			mount_component(statcard4, div, null);
			append(div, t4);
			mount_component(statcard5, div, null);
			append(div, t5);
			mount_component(statcard6, div, null);
			append(div, t6);
			mount_component(statcard7, div, null);
			append(div, t7);
			mount_component(statcard8, div, null);
			append(div, t8);
			mount_component(statcard9, div, null);
			current = true;
		},
		p(ctx, dirty) {
			const statcard0_changes = {};
			if (dirty & /*dashboard*/ 1) statcard0_changes.value = /*dashboard*/ ctx[0].proxy_slots || 0;
			statcard0.$set(statcard0_changes);
			const statcard1_changes = {};
			if (dirty & /*dashboard*/ 1) statcard1_changes.value = /*dashboard*/ ctx[0].active || 0;
			if (dirty & /*dashboard*/ 1) statcard1_changes.sub = '目标池：' + (/*dashboard*/ ctx[0].target_pool || 0);
			statcard1.$set(statcard1_changes);
			const statcard2_changes = {};
			if (dirty & /*dashboard*/ 1) statcard2_changes.value = /*dashboard*/ ctx[0].unique_ips || 0;
			statcard2.$set(statcard2_changes);
			const statcard3_changes = {};
			if (dirty & /*dashboard*/ 1) statcard3_changes.value = /*dashboard*/ ctx[0].running_tunnels || 0;
			statcard3.$set(statcard3_changes);
			const statcard4_changes = {};
			if (dirty & /*dashboard*/ 1) statcard4_changes.value = /*dashboard*/ ctx[0].proxy_port || '-';
			statcard4.$set(statcard4_changes);
			const statcard5_changes = {};
			if (dirty & /*dashboard*/ 1) statcard5_changes.value = /*dashboard*/ ctx[0].total || 0;
			if (dirty & /*dashboard*/ 1) statcard5_changes.sub = '停用：' + (/*dashboard*/ ctx[0].disabled || 0) + ' / 错误：' + (/*dashboard*/ ctx[0].error || 0);
			statcard5.$set(statcard5_changes);
			const statcard6_changes = {};
			if (dirty & /*dashboard*/ 1) statcard6_changes.value = fmtBytes(/*dashboard*/ ctx[0].total_up || 0);
			statcard6.$set(statcard6_changes);
			const statcard7_changes = {};
			if (dirty & /*dashboard*/ 1) statcard7_changes.value = fmtBytes(/*dashboard*/ ctx[0].total_down || 0);
			statcard7.$set(statcard7_changes);
			const statcard8_changes = {};
			if (dirty & /*dashboard*/ 1) statcard8_changes.value = /*dashboard*/ ctx[0].running ? '运行中' : '空闲';
			if (dirty & /*lastRun*/ 8) statcard8_changes.sub = '上次：' + /*lastRun*/ ctx[3];
			statcard8.$set(statcard8_changes);
			const statcard9_changes = {};
			if (dirty & /*nextRun*/ 4) statcard9_changes.value = /*nextRun*/ ctx[2];
			statcard9.$set(statcard9_changes);
		},
		i(local) {
			if (current) return;
			transition_in(statcard0.$$.fragment, local);
			transition_in(statcard1.$$.fragment, local);
			transition_in(statcard2.$$.fragment, local);
			transition_in(statcard3.$$.fragment, local);
			transition_in(statcard4.$$.fragment, local);
			transition_in(statcard5.$$.fragment, local);
			transition_in(statcard6.$$.fragment, local);
			transition_in(statcard7.$$.fragment, local);
			transition_in(statcard8.$$.fragment, local);
			transition_in(statcard9.$$.fragment, local);
			current = true;
		},
		o(local) {
			transition_out(statcard0.$$.fragment, local);
			transition_out(statcard1.$$.fragment, local);
			transition_out(statcard2.$$.fragment, local);
			transition_out(statcard3.$$.fragment, local);
			transition_out(statcard4.$$.fragment, local);
			transition_out(statcard5.$$.fragment, local);
			transition_out(statcard6.$$.fragment, local);
			transition_out(statcard7.$$.fragment, local);
			transition_out(statcard8.$$.fragment, local);
			transition_out(statcard9.$$.fragment, local);
			current = false;
		},
		d(detaching) {
			if (detaching) detach(div);
			destroy_component(statcard0);
			destroy_component(statcard1);
			destroy_component(statcard2);
			destroy_component(statcard3);
			destroy_component(statcard4);
			destroy_component(statcard5);
			destroy_component(statcard6);
			destroy_component(statcard7);
			destroy_component(statcard8);
			destroy_component(statcard9);
		}
	};
}

// (30:0) {#if error}
function create_if_block(ctx) {
	let div;
	let t0;
	let t1;

	return {
		c() {
			div = element("div");
			t0 = text("加载失败：");
			t1 = text(/*error*/ ctx[1]);
			attr(div, "class", "alert");
		},
		m(target, anchor) {
			insert(target, div, anchor);
			append(div, t0);
			append(div, t1);
		},
		p(ctx, dirty) {
			if (dirty & /*error*/ 2) set_data(t1, /*error*/ ctx[1]);
		},
		i: noop,
		o: noop,
		d(detaching) {
			if (detaching) detach(div);
		}
	};
}

function create_fragment(ctx) {
	let h2;
	let t1;
	let current_block_type_index;
	let if_block;
	let if_block_anchor;
	let current;
	const if_block_creators = [create_if_block, create_if_block_1, create_else_block];
	const if_blocks = [];

	function select_block_type(ctx, dirty) {
		if (/*error*/ ctx[1]) return 0;
		if (/*dashboard*/ ctx[0]) return 1;
		return 2;
	}

	current_block_type_index = select_block_type(ctx, -1);
	if_block = if_blocks[current_block_type_index] = if_block_creators[current_block_type_index](ctx);

	return {
		c() {
			h2 = element("h2");
			h2.textContent = "仪表盘";
			t1 = space();
			if_block.c();
			if_block_anchor = empty();
		},
		m(target, anchor) {
			insert(target, h2, anchor);
			insert(target, t1, anchor);
			if_blocks[current_block_type_index].m(target, anchor);
			insert(target, if_block_anchor, anchor);
			current = true;
		},
		p(ctx, [dirty]) {
			let previous_block_index = current_block_type_index;
			current_block_type_index = select_block_type(ctx, dirty);

			if (current_block_type_index === previous_block_index) {
				if_blocks[current_block_type_index].p(ctx, dirty);
			} else {
				group_outros();

				transition_out(if_blocks[previous_block_index], 1, 1, () => {
					if_blocks[previous_block_index] = null;
				});

				check_outros();
				if_block = if_blocks[current_block_type_index];

				if (!if_block) {
					if_block = if_blocks[current_block_type_index] = if_block_creators[current_block_type_index](ctx);
					if_block.c();
				} else {
					if_block.p(ctx, dirty);
				}

				transition_in(if_block, 1);
				if_block.m(if_block_anchor.parentNode, if_block_anchor);
			}
		},
		i(local) {
			if (current) return;
			transition_in(if_block);
			current = true;
		},
		o(local) {
			transition_out(if_block);
			current = false;
		},
		d(detaching) {
			if (detaching) detach(h2);
			if (detaching) detach(t1);
			if_blocks[current_block_type_index].d(detaching);
			if (detaching) detach(if_block_anchor);
		}
	};
}

function instance($$self, $$props, $$invalidate) {
	let lastRun;
	let nextRun;
	let dashboard = null;
	let error = '';

	async function loadDashboard() {
		try {
			$$invalidate(0, dashboard = await fetchJSON('/api/dashboard/json'));
			$$invalidate(1, error = '');
		} catch(err) {
			$$invalidate(1, error = err.message);
		}
	}

	onMount(() => {
		loadDashboard();
		const timer = setInterval(loadDashboard, 5000);
		return () => clearInterval(timer);
	});

	$$self.$$.update = () => {
		if ($$self.$$.dirty & /*dashboard*/ 1) {
			$: $$invalidate(3, lastRun = dashboard ? fmtTime(dashboard.last_run_at, '从未') : '');
		}

		if ($$self.$$.dirty & /*dashboard*/ 1) {
			$: $$invalidate(2, nextRun = dashboard ? fmtTime(dashboard.next_run_at, '-') : '');
		}
	};

	return [dashboard, error, nextRun, lastRun];
}

class Dashboard extends SvelteComponent {
	constructor(options) {
		super();
		init(this, options, instance, create_fragment, safe_not_equal, {});
	}
}

export default Dashboard;