/* src\components\StatusTag.svelte generated by Svelte v3.59.2 */
import {
	SvelteComponent,
	append,
	attr,
	detach,
	element,
	init,
	insert,
	noop,
	safe_not_equal,
	set_data,
	text
} from "svelte/internal";

import { tagLabel } from '../lib/status.js';

function create_fragment(ctx) {
	let span;
	let t_value = tagLabel(/*status*/ ctx[0]) + "";
	let t;
	let span_class_value;

	return {
		c() {
			span = element("span");
			t = text(t_value);
			attr(span, "class", span_class_value = 'tag ' + /*status*/ ctx[0]);
		},
		m(target, anchor) {
			insert(target, span, anchor);
			append(span, t);
		},
		p(ctx, [dirty]) {
			if (dirty & /*status*/ 1 && t_value !== (t_value = tagLabel(/*status*/ ctx[0]) + "")) set_data(t, t_value);

			if (dirty & /*status*/ 1 && span_class_value !== (span_class_value = 'tag ' + /*status*/ ctx[0])) {
				attr(span, "class", span_class_value);
			}
		},
		i: noop,
		o: noop,
		d(detaching) {
			if (detaching) detach(span);
		}
	};
}

function instance($$self, $$props, $$invalidate) {
	let { status } = $$props;

	$$self.$$set = $$props => {
		if ('status' in $$props) $$invalidate(0, status = $$props.status);
	};

	return [status];
}

class StatusTag extends SvelteComponent {
	constructor(options) {
		super();
		init(this, options, instance, create_fragment, safe_not_equal, { status: 0 });
	}
}

export default StatusTag;